sudo apt update
sudo apt install -y python3-venv python3-pip

python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install flask gunicorn google-cloud-firestore

sudo apt install -y git
git --version

cd ~
git clone https://github.com/mmamei/PervasiveCloud.git
git clone https://github.com/romano-francesco1/provapcloud2.git
cd PervasiveCloud
cd provapcloud2

export PORT=8080
python main.py

DAL MIO PC:
python client_iot.py --base ./fatigueset --server-base http://<VM_EXTERNAL_IP> --endpoint /data